{
  "name": "@lexops/geofencing",
  "version": "0.1.0",
  "private": true,
  "description": "محرك Geo السيادي — Haversine + نصف قطر أمان + كشف التزييف المكاني (الطبقة المادية في Triple-Path)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^2.1.9"
  }
}
