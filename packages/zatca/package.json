{
  "name": "@lexops/zatca",
  "version": "0.1.0",
  "private": true,
  "description": "ختم ZATCA — الفوترة الإلكترونية: UBL 2.1 + CSID + هاش الفاتورة (SHA-256) + QR TLV",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@lexops/c9-ledger": "workspace:*",
    "@lexops/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "vitest": "^2.1.9"
  }
}
