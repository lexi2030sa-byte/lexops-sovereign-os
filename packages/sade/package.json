{
  "name": "@lexops/sade",
  "version": "0.1.0",
  "private": true,
  "description": "SADE Orchestrator — محرك التوثيق الذاتي السيادي: ربط زنادات محرك القواعد بـ Document Generator وختم C9",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@lexops/c9-ledger": "workspace:*",
    "@lexops/contracts": "workspace:*",
    "@lexops/rule-engine": "workspace:*",
    "@lexops/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "vitest": "^2.1.9"
  }
}
