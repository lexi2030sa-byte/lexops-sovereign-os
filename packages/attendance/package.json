{
  "name": "@lexops/attendance",
  "version": "0.1.0",
  "private": true,
  "description": "جداول PEOPLE و ATTENDANCE (Blueprints) + GeoGate — خدمة الحضور الجغرافي السيادي مع كشف التلاعب",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@lexops/c9-ledger": "workspace:*",
    "@lexops/geofencing": "workspace:*",
    "@lexops/shared": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^2.1.9"
  }
}
