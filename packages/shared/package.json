{
  "name": "@lexops/shared",
  "version": "0.1.0",
  "private": true,
  "description": "الثوابت والأنواع المشتركة — دوستور النواة (المواعيد النظامية، عتبات اليقين، المصنفات، النطاقات)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
