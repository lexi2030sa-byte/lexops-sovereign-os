{
  "name": "@lexops/rule-engine",
  "version": "0.1.0",
  "private": true,
  "description": "محرك قواعد الامتثال — جسر JSON Logic على legal_logic_rules.json مع دوستور القواعد (Lexi)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "generate:config": "tsx scripts/generate-config.ts",
    "load:firestore": "tsx scripts/load-firestore.ts"
  },
  "dependencies": {
    "json-logic-js": "^2.0.2",
    "@lexops/shared": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^2.0.0"
  }
}
